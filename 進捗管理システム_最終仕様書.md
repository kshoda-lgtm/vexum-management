# skecheck - スケジュール管理システム 仕様書

## 1. システム概要

### 1.1 目的
スマホとPCで同期できる進捗管理・スケジュール管理システム。クライアント先に常駐するスタッフのスケジュール・達成度を管理し、月次レポートを自動生成できるシステム。

### 1.2 システム構成
- **フロントエンド**: React 19 + Vite
- **スタイリング**: Tailwind CSS v3
- **バックエンド**: Supabase (PostgreSQL + Realtime Database)
- **アイコン**: Lucide React
- **日付処理**: date-fns
- **デプロイ**: GitHub Pages

### 1.3 デプロイ情報
- **本番URL**: https://kshoda-lgtm.github.io/vexum-management/
- **リポジトリ**: https://github.com/kshoda-lgtm/vexum-management
- **Supabase URL**: https://uzmndwlqsrsghvwzgkqm.supabase.co

---

## 2. 核心機能

### 2.1 本日のタスク
- シンプルなタスクリスト（LocalStorageで管理）
- 終了1週間前のタスク自動通知
- 日付が変わると自動リセット
- デフォルトで表示される画面

### 2.2 スタッフ管理
- スタッフ情報の登録・編集・削除
- プロジェクトごとの担当者管理
- スタッフ別の稼働状況

### 2.3 スケジュール管理
- タスクの登録・進捗管理
- 担当者の割り当て
- ステータス管理（未着手/進行中/完了/遅延）
- 達成度（0-100%）の管理

### 2.4 ミーティング議事録
- 会議内容の記録
- 参加者・決定事項の管理
- タスクとの紐付け

### 2.5 月次レポート
- 自動レポート生成
- タスク完了率の集計
- html2canvasによる画像生成

### 2.6 シフト管理
- カレンダー形式のシフト表
- スクリーンショット保存機能

### 2.7 タスクタイムライン
- カレンダービューとリストビュー
- タスクの色分け表示
- 期限による視覚的管理

### 2.8 メモ
- 簡易メモ機能
- LocalStorageで管理

### 2.9 データ同期
- Supabase Realtime Databaseによる自動同期
- スマホ↔PC間でリアルタイム同期
- 無料枠制限の自動検知と通知

### 2.10 バックアップ管理
- JSON形式でのエクスポート・インポート
- データの復元機能

---

## 3. データモデル

### 3.1 データベース構造（Supabase）

Supabaseのテーブル構造：
```sql
CREATE TABLE app_data (
  id INTEGER PRIMARY KEY,
  staff JSONB DEFAULT '[]',
  tasks JSONB DEFAULT '[]',
  meetings JSONB DEFAULT '[]',
  reports JSONB DEFAULT '[]',
  shifts JSONB DEFAULT '[]',
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 3.2 Staff（スタッフ情報）
```javascript
{
  id: string,                    // UUID
  name: string,                  // 氏名
  email: string,                 // メールアドレス
  phone: string,                 // 電話番号
  department: string,            // 部署
  position: string,              // 役職
  joinDate: string,              // 入社日
  skills: string[],              // スキル
  currentProject: string,        // 現在のプロジェクト
  notes: string,                 // 備考
  createdAt: string,
  updatedAt: string
}
```

### 3.3 Task（タスク情報）
```javascript
{
  id: string,                    // UUID
  taskName: string,              // タスク名
  description: string,           // 説明
  staffId: string,               // 担当スタッフID
  staffName: string,             // 担当スタッフ名
  projectName: string,           // プロジェクト名
  deadline: string,              // 期限日 (YYYY-MM-DD)
  status: string,                // not_started | in_progress | completed | delayed
  completionRate: number,        // 達成度 0-100
  priority: string,              // low | medium | high
  tags: string[],                // タグ
  notes: string,                 // 備考
  createdAt: string,
  updatedAt: string
}
```

### 3.4 Meeting（ミーティング記録）
```javascript
{
  id: string,                    // UUID
  title: string,                 // ミーティング名
  date: string,                  // 日時
  participants: string[],        // 参加者
  agenda: string,                // 議題
  decisions: string,             // 決定事項
  notes: string,                 // メモ
  nextMeeting: string,           // 次回ミーティング日時
  createdAt: string,
  updatedAt: string
}
```

### 3.5 Report（月次レポート）
```javascript
{
  id: string,                    // UUID
  month: string,                 // 対象月 (YYYY-MM)
  staffId: string,               // スタッフID
  summary: string,               // サマリー
  achievements: string,          // 達成事項
  issues: string,                // 課題
  nextMonthPlan: string,         // 来月の計画
  imageUrl: string,              // 生成した画像URL（オプション）
  createdAt: string,
  updatedAt: string
}
```

### 3.6 Shift（シフト情報）
```javascript
{
  id: string,                    // UUID
  staffId: string,               // スタッフID
  staffName: string,             // スタッフ名
  date: string,                  // 日付 (YYYY-MM-DD)
  shiftType: string,             // morning | afternoon | night | off
  startTime: string,             // 開始時刻
  endTime: string,               // 終了時刻
  notes: string,                 // 備考
  createdAt: string,
  updatedAt: string
}
```

---

## 4. データ同期（Supabase）

### 4.1 Supabase設定

```javascript
// src/supabase.js
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'https://uzmndwlqsrsghvwzgkqm.supabase.co';
const supabaseAnonKey = 'your-anon-key';

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

### 4.2 データ操作

#### データの読み込み
```javascript
const { data, error } = await supabase
  .from('app_data')
  .select('*')
  .eq('id', 1)
  .single();
```

#### データの保存
```javascript
const { error } = await supabase
  .from('app_data')
  .upsert({
    id: 1,
    staff: staffArray,
    tasks: tasksArray,
    meetings: meetingsArray,
    reports: reportsArray,
    shifts: shiftsArray,
    updated_at: new Date().toISOString()
  });
```

### 4.3 リアルタイム同期

```javascript
const channel = supabase
  .channel('app_data_changes')
  .on('postgres_changes',
    { event: '*', schema: 'public', table: 'app_data' },
    (payload) => {
      // データが更新されたときの処理
      if (payload.new) {
        setStaff(payload.new.staff || []);
        setTasks(payload.new.tasks || []);
        // ...
      }
    }
  )
  .subscribe();
```

### 4.4 無料枠の制限

- **データベース容量**: 500MB
- **帯域幅**: 5GB/月
- **リアルタイム接続**: 同時200接続まで

上限到達時は自動停止し、エラー通知を表示します。

---

## 5. フロントエンド実装

### 5.1 技術スタック
- **フレームワーク**: React 19 + Vite
- **スタイリング**: Tailwind CSS v3
- **状態管理**: React Context + Supabase Realtime
- **アイコン**: Lucide React
- **日付処理**: date-fns
- **画像生成**: html2canvas
- **チャート**: Recharts
- **ファイル保存**: file-saver

### 5.2 主要コンポーネント

#### Header.jsx
ナビゲーションバー。各機能への切り替えボタンを提供。

#### TodayTasks.jsx
本日のタスク画面。LocalStorageで管理され、終了1週間前のタスクを自動表示。

#### StaffList.jsx + StaffModal.jsx
スタッフ一覧と編集モーダル。スタッフ情報のCRUD操作。

#### ScheduleList.jsx
スケジュール一覧。タスクの進捗状況を一覧表示。

#### TaskCard.jsx + TaskModal.jsx
タスクカードとタスク編集モーダル。

#### MeetingList.jsx + MeetingModal.jsx
ミーティング一覧と編集モーダル。議事録の記録。

#### ReportGenerator.jsx
月次レポート生成画面。html2canvasで画像を生成。

#### ShiftSchedule.jsx
シフトスケジュール。カレンダー形式でシフトを管理。

#### TaskTimeline.jsx
タスクタイムライン。カレンダービューとリストビューを提供。

#### Memo.jsx
簡易メモ機能。LocalStorageで管理。

#### BackupManager.jsx
バックアップ管理。JSON形式でのエクスポート・インポート。

#### ErrorNotification.jsx
エラー通知コンポーネント。Supabaseのエラーなどを表示。

### 5.3 状態管理（AppContext）

```javascript
// src/contexts/AppContext.jsx
export const AppProvider = ({ children }) => {
  const [staff, setStaff] = useState([]);
  const [tasks, setTasks] = useState([]);
  const [meetings, setMeetings] = useState([]);
  const [reports, setReports] = useState([]);
  const [shifts, setShifts] = useState([]);

  // Supabaseからデータを読み込み
  // リアルタイム同期を設定
  // ...
}
```

---

## 6. デプロイ情報

### 6.1 Supabaseセットアップ
1. Supabaseプロジェクトを作成
2. SQLエディタで `app_data` テーブルを作成
3. `src/supabase.js` にプロジェクトURLとAnon Keyを設定

### 6.2 GitHub Pagesデプロイ

**デプロイ済み**: ✅
**URL**: https://kshoda-lgtm.github.io/vexum-management/

**デプロイ手順**:
```bash
# ビルド
npm run build

# distフォルダをgh-pagesブランチにデプロイ
cd dist
git init
git add -A
git commit -m "deploy"
git push -f https://github.com/kshoda-lgtm/vexum-management.git master:gh-pages
cd ..
```

### 6.3 開発環境

**開発サーバー起動**:
```bash
npm install
npm run dev
```

**ビルド**:
```bash
npm run build
```

**プレビュー**:
```bash
npm run preview
```

---

## 7. ファイル構成

```
進捗管理/
├── src/
│   ├── components/           # Reactコンポーネント
│   │   ├── Header.jsx
│   │   ├── TodayTasks.jsx
│   │   ├── StaffList.jsx
│   │   ├── StaffModal.jsx
│   │   ├── ScheduleList.jsx
│   │   ├── TaskCard.jsx
│   │   ├── TaskModal.jsx
│   │   ├── MeetingList.jsx
│   │   ├── MeetingModal.jsx
│   │   ├── ReportGenerator.jsx
│   │   ├── ShiftSchedule.jsx
│   │   ├── TaskTimeline.jsx
│   │   ├── Memo.jsx
│   │   ├── BackupManager.jsx
│   │   └── ErrorNotification.jsx
│   ├── contexts/
│   │   └── AppContext.jsx    # 状態管理
│   ├── utils/
│   │   ├── storage.js        # LocalStorage操作
│   │   └── initSupabase.js   # Supabase初期化
│   ├── config/
│   │   └── api.js            # API設定
│   ├── supabase.js           # Supabase設定
│   ├── App.jsx               # メインアプリ
│   └── main.jsx              # エントリーポイント
├── public/                   # 静的ファイル
├── gas-backend/              # 旧GASバックエンド（非推奨）
├── dist/                     # ビルド出力
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
├── eslint.config.js
├── .gitignore
├── README.md
└── 進捗管理システム_最終仕様書.md  # この仕様書
```

---

## 8. 開発フェーズ

### Phase 1: Supabaseセットアップ ✅
- Supabaseプロジェクト作成
- データベーステーブル設計・作成
- リアルタイム同期設定

### Phase 2: フロントエンド基本機能 ✅
- スタッフ管理CRUD
- タスク管理CRUD
- Supabaseとの接続

### Phase 3: ミーティング記録機能 ✅
- ミーティング記録CRUD
- タスクとの紐付け

### Phase 4: 月次レポート機能 ✅
- 月次レポート作成画面
- 自動データ取得
- html2canvasによる画像生成

### Phase 5: シフト管理・タイムライン ✅
- シフトスケジュール
- タスクタイムライン
- カレンダービュー

### Phase 6: 本日のタスク機能 ✅
- 本日のタスク画面
- 終了1週間前の自動通知
- 日付自動リセット

### Phase 7: デプロイ ✅
- GitHub Pagesデプロイ
- リアルタイム同期動作確認

---

## 9. 今後の改善点

1. **機能追加**
   - タスクの通知機能（メール・LINE連携）
   - より詳細なレポート分析
   - ダッシュボード画面

2. **UI/UX改善**
   - ダークモード対応
   - アニメーション追加
   - さらなるレスポンシブ対応

3. **パフォーマンス最適化**
   - 大量データの処理最適化
   - 画像生成の高速化
   - キャッシュ戦略の改善

4. **セキュリティ**
   - 認証機能の追加（Supabase Auth）
   - 権限管理の実装

---

## 10. 参考資料

### 本番環境
- **本番URL**: https://kshoda-lgtm.github.io/vexum-management/
- **GitHubリポジトリ**: https://github.com/kshoda-lgtm/vexum-management

### Supabase
- **Supabaseプロジェクト**: https://uzmndwlqsrsghvwzgkqm.supabase.co
- **ドキュメント**: https://supabase.com/docs

### フレームワーク・ライブラリ
- **React**: https://react.dev/
- **Vite**: https://vitejs.dev/
- **Tailwind CSS**: https://tailwindcss.com/
- **Lucide Icons**: https://lucide.dev/
- **date-fns**: https://date-fns.org/
- **Recharts**: https://recharts.org/

---

**更新日**: 2025-10-19
**バージョン**: 2.0
**ステータス**: 本番稼働中（GitHub Pages + Supabase）
